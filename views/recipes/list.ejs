<h1>Recipes List</h1>
<% recipes.forEach(recipe => { %>
    <div class="recipe">
        <h2><%= recipe.title %></h2>
        <p><%= recipe.description %></p>
        <a href="/recipes/view/<%= recipe._id %>">View Recipe</a>
    </div>
<% }) %>
